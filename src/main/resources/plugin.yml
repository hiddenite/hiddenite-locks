main: eu.hiddenite.locks.LocksPlugin
name: HiddeniteLocks
version: 1.2.0
api-version: 1.18
author: Hiddenite
commands:
  lock:
    description: "Locks a container."
    usage: /lock [+/-/?]
  unlock:
    description: "Unlocks a container."
    usage: /unlock
permissions:
  hiddenite.locks.bypass:
    description: "Can access and unlock locked containers."
    default: op
